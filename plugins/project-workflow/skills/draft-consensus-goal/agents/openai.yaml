interface:
  display_name: "Draft Consensus Goal"
  short_description: "Turn accepted discussion decisions into GOAL content"
  default_prompt: "使用 $project-workflow:draft-consensus-goal 将当前讨论中已接受的决定整理为中文 GOAL 内容，但不要启动 GOAL。"

policy:
  allow_implicit_invocation: false
