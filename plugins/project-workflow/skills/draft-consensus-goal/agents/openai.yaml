interface:
  display_name: "Draft Consensus Goal"
  short_description: "把讨论中已接受的决定整理为可验证的中文 GOAL 内容"
  default_prompt: "使用 $project-workflow:draft-consensus-goal 将当前讨论中已接受的决定整理为可验证的中文 GOAL 内容，但不要创建或启动 GOAL。"

policy:
  allow_implicit_invocation: false
